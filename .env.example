# Copy this file to .env (already gitignored -- never commit real secrets)
# and fill these in, or just run scripts/get-refresh-token.py /
# scripts/get-refresh-token.ps1, which will prompt for missing/blank
# values and fill in CHROME_REFRESH_TOKEN automatically.
#
# See docs/chrome-web-store-deploy.md for where each value comes from.

CHROME_EXTENSION_ID=
CHROME_CLIENT_ID=
CHROME_CLIENT_SECRET=
CHROME_REFRESH_TOKEN=
